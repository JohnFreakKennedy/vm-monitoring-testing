apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: "{{ include "myApp.fullname" . }}-clusterrolebinding"
  labels:
    {{- include "myApp.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: "{{ include "myApp.fullname" . }}-clusterrole"
subjects:
  - kind: ServiceAccount
    name: "{{ include "myApp.serviceAccountName" . | trim }}"  
    namespace: "{{ .Release.Namespace }}"